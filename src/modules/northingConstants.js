/** Where to return from /property/:id Back (set when opening a listing from feed or home). */
export const PROPERTY_BACK_STORAGE_KEY = "northingPropertyBack";

/** After sending an enquiry on /property/:id, buyer dashboard opens Enquiries tab once. */
export const OPEN_ENQUIRIES_TAB_STORAGE_KEY = "northing_open_enquiries_tab";
