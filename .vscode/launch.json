{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open app — localhost:3000 (run npm run dev first)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}"
    },
    {
      "name": "Open app — 127.0.0.1:3000 (run npm run dev first)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://127.0.0.1:3000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
